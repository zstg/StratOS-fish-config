* Add this to flake.nix:
#+BEGIN_SRC bash
{
  inputs.stratos-fish.url = "github:zstg/StratOS-fish-config";
  
  outputs = { self, nixpkgs, stratos-fish, ... }: {
    nixosSystem = {
      modules = [
        stratos-fish.nixosModules.default
      ];
    };
  };
}
#+END_SRC