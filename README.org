* Add this to flake.nix:
#+BEGIN_SRC bash
stratos-fish.url = "github:StratOS-Linux/StratOS-fish-config";
#+END_SRC


then something like this in your home-manager-config:
#+BEGIN_SRC bash
{
  imports = [ inputs.stratos-fish.homeManagerModules.default ];
  
  services.stratos-fish.enable = true;
}
#+END_SRC